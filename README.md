# excelgrader
Strict, browser-based grader that checks a submitted .xlsx against an answer key — cell-by-cell diffing, optional order-independent sheets/rows, and a downloadable text report. No server, no uploads — runs entirely client-side.
